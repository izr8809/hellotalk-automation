# 변경 이력

## 2026-01-26 - 주요 업데이트

### 1. ✅ 일일 제한 완전 제거

**이전 문제:**
- 하루에 20명까지만 신규 유저에게 첫 메시지 전송 가능
- 20명 도달 후 나머지 신규 유저들은 무시됨
- 50+ 유저가 있어도 20명만 처리

**해결:**
- `daily_limit` 변수 완전 제거
- 모든 신규 유저에게 제한 없이 첫 메시지 전송
- 유저 수 제한 없음

**변경된 코드:**
```python
# 이전
if self.today_started >= self.daily_limit:
    print(f"⚠️ 오늘 제한 도달 ({self.daily_limit}명)")
    return False

# 현재
# 제한 체크 완전 제거
print(f"📤 첫 메시지 전송: {username}")
```

---

### 2. ✅ 배치 처리 시스템 도입

**이전 문제:**
- 모든 유저를 한번에 처리 시도
- 중간에 쉬지 않고 계속 진행
- 유저가 많으면 봇이 과부하

**해결:**
- 30명씩 배치로 나누어 처리
- 각 배치 사이에 30초 휴식
- 전체 완주 후 60초 대기

**처리 플로우:**
```
사이클 N:
├─ 배치 1 (30명) 처리
│  └─ 30초 휴식
├─ 배치 2 (30명) 처리  
│  └─ 30초 휴식
├─ 배치 3 (나머지) 처리
└─ 60초 대기 후 다음 사이클
```

**예시 출력:**
```
============================================================
📦 배치 1/3: 30명 처리 중
============================================================

[1/85] 🆕 신규 | User1 (단계: 0/8)
[2/85] 🔴 진행 | User2 (단계: 3/8)
...
[30/85] ⚪ 진행 | User30 (단계: 5/8)

============================================================
✅ 배치 1/3 완료: 30명 체크, 15개 액션
============================================================

⏸️  잠시 휴식... (남은 유저: 55명)
⏳ 30초 후 다음 배치 시작...
```

---

### 3. ✅ 전체 유저 완주 보장

**이전 문제:**
- 같은 유저들만 반복 체크
- 뒤쪽 유저들은 영원히 무시됨

**해결:**
- 모든 신규 + 진행 중인 유저를 `users_to_check`에 추가
- 배치 단위로 끝까지 완주
- 다음 사이클에서 처음부터 다시 시작

**변경된 코드:**
```python
# 이전
if self.today_started < self.daily_limit and not_started_yet:
    new_to_start = not_started_yet[:self.daily_limit - self.today_started]
    users_to_check.extend(new_to_start)

# 현재
users_to_check.extend(not_started_yet)  # 모든 신규 유저
users_to_check.extend(in_progress)       # 모든 진행 중인 유저
```

---

## 성능 비교

### 이전 시스템 (문제)

```
유저: 85명 (60명 신규 + 25명 진행 중)

사이클 1:
- 신규 20명 처리 (일일 제한)
- 진행 중 0명
- 총 20명 처리
- 60초 대기

사이클 2:
- 신규 0명 (제한 도달)
- 진행 중 20명 처리
- 총 20명 처리
- 60초 대기

사이클 3+:
- 같은 20명만 반복 체크
- 나머지 40명 신규 유저 무시됨 ❌
```

### 현재 시스템 (개선)

```
유저: 85명 (60명 신규 + 25명 진행 중)

사이클 1:
- 배치 1: 30명 처리 → 30초 휴식
- 배치 2: 30명 처리 → 30초 휴식
- 배치 3: 25명 처리
- 총 85명 전부 처리 ✅
- 60초 대기

사이클 2:
- 모든 유저 다시 체크
- 신규 유저도 계속 추가 가능
```

---

## 새로운 기능

### 배치 진행 상황 표시

```
============================================================
[사이클 3] 16:30:45
============================================================
읽지 않은 메시지: 35명 | 진행 중: 60명 | 완료: 15명 | 신규: 10명

🆕 신규 유저: 10명
🔄 진행 중: 60명

🔄 전체 70명 스윕 시작:

============================================================
📦 배치 1/3: 30명 처리 중
============================================================

[1/70] 🆕 신규 | John (단계: 0/8)
...

============================================================
✅ 배치 1/3 완료: 30명 체크, 12개 액션
============================================================

⏸️  잠시 휴식... (남은 유저: 40명)
⏳ 30초 후 다음 배치 시작...
```

---

## 설정 가능한 값

### 배치 크기 조정

`multi_user_bot.py`의 `BATCH_SIZE` 변수를 수정:

```python
BATCH_SIZE = 30  # 기본값: 30명씩 처리
```

**권장 값:**
- 10-20명: 안정적이지만 느림
- 30-50명: 균형잡힌 성능 (권장)
- 50+명: 빠르지만 과부하 위험

### 휴식 시간 조정

```python
# 배치 사이 휴식 (기본: 30초)
time.sleep(30)

# 사이클 사이 대기 (기본: 60초)
time.sleep(60)
```

---

## 제거된 기능

### bot_state.json 변경

**이전:**
```json
{
  "user_states": {...},
  "today_started": 20,
  "current_date": "2026-01-26"
}
```

**현재:**
```json
{
  "user_states": {...},
  "current_date": "2026-01-26"
}
```

`today_started` 필드 제거됨 (더 이상 필요 없음)

---

## 업그레이드 가이드

### 기존 bot_state.json 호환성

기존 `bot_state.json` 파일이 있어도 자동으로 작동합니다.
- `today_started` 필드는 무시됨
- `user_states`는 그대로 유지

### 주의사항

1. **Chrome 메모리**: 유저가 많으면 Chrome 메모리 사용량 증가
2. **API 제한**: HelloTalk에서 rate limiting이 있을 수 있음
3. **배치 크기**: 너무 크면 과부하, 너무 작으면 비효율

---

## 향후 계획

- [ ] 스마트 대기: 모든 유저가 답장 대기 중일 때만 긴 대기
- [ ] 우선순위 시스템: 읽지 않은 메시지가 있는 유저 먼저
- [ ] 에러 복구: 네트워크 오류 시 자동 재시도
- [ ] 통계 대시보드: 실시간 진행 상황 시각화
